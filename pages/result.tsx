/**
 * Result — Per-decision result: criterion-level scores, evidence spans, structural-verifier outputs. Second-audience-facing; authority view is similar with the commit affordance. See the framework paper §6.
 */
export default function Result() {
  // TODO: implement per the JSDoc above.
  return (
    <div className="min-h-screen p-8">
      <h1 className="text-2xl font-bold text-slate-900">Result</h1>
      <p className="mt-2 text-slate-600">Scaffold — see README and the framework paper for spec.</p>
    </div>
  );
}
