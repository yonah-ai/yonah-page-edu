/**
 * AuthorityDashboard — Authority-audience home: draft + published shared-contract artefacts, bound cohorts, pending decisions to commit. Calls authority-side tools only. See the framework paper §6 Authority flow.
 */
export default function AuthorityDashboard() {
  // TODO: implement per the JSDoc above.
  return (
    <div className="min-h-screen p-8">
      <h1 className="text-2xl font-bold text-slate-900">AuthorityDashboard</h1>
      <p className="mt-2 text-slate-600">Scaffold — see README and the framework paper for spec.</p>
    </div>
  );
}
