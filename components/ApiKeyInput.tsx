/**
 * ApiKeyInput — API key text input with provider auto-detect (sk-ant-* / sk-* / AIza*). Stores masked key in session storage; never persists.
 */
export default function ApiKeyInput() {
  // TODO: implement per the JSDoc above.
  return null;
}
