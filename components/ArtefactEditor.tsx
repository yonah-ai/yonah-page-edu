/**
 * ArtefactEditor — Per-shape editor for the shared-contract artefact (framework base).
 *
 * Framework default: renders a generic criterion-list editor. Vertical
 * forks override this with vertical-specific editors (RubricEditor for
 * the education vertical, CareProtocolEditor for health, JobCriteriaEditor
 * for hire).
 */
export default function ArtefactEditor() {
  // TODO: implement per the JSDoc above.
  return null;
}
