/**
 * Pipeline — Live envelope-flow visualisation (react-flow). Subscribes to WSS envelope stream. Role-aware view filtering. See the framework paper §5 + figure 2.
 */
export default function Pipeline() {
  // TODO: implement per the JSDoc above.
  return (
    <div className="min-h-screen p-8">
      <h1 className="text-2xl font-bold text-slate-900">Pipeline</h1>
      <p className="mt-2 text-slate-600">Scaffold — see README and the framework paper for spec.</p>
    </div>
  );
}
