/**
 * Landing — Introduces Yonah and routes new visitors to login. See README + the framework paper at ACM TAAS.
 *
 * Framework default: explains the seven-tool surface, the three-crew structure,
 * and the per-decision provenance commitment in vertical-agnostic language.
 * Vertical forks override this landing with vertical-specific copy.
 */
export default function Landing() {
  // TODO: implement per the JSDoc above.
  return (
    <div className="min-h-screen p-8">
      <h1 className="text-2xl font-bold text-slate-900">Landing</h1>
      <p className="mt-2 text-slate-600">Scaffold — see README and the framework paper for spec.</p>
    </div>
  );
}
