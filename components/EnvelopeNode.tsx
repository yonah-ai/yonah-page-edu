/**
 * EnvelopeNode — Custom react-flow node: agent label, activity timestamp, structural-verifier status pill (green/red), envelope content-hash prefix. Click → opens side panel with full envelope JSON.
 */
export default function EnvelopeNode() {
  // TODO: implement per the JSDoc above.
  return null;
}
